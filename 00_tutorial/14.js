// import module
import * as printHelper from './helpers.js'

const container = document.getElementById("container14");

// Example 1
printHelper.addChild(container, `<code>person.toString():</code>${'hi'}`)